Use this library to document your django apis with raml (http://raml.org/) it is being developed, with python decorators.
Note this library also envforces the documented JSON Schema (http://json-schema.org/) to validate input data.  

See the unit_test for example use.